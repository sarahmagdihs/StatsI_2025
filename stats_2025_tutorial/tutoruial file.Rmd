---
title: "tutorial_1"
author: "Sarah Magdihs"
date: "2025-09-23"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#load data 
```{r}
getwd()
df <- read.csv("/Users/sarahmagdihs//Documents/GitHub/StatsI_2025/datasets/fictional_data.csv")
df

##full path needed since its not in STATSI_2025; but in a subfolder ---> ask before homework submission if that makes a difference and if they want me to upload it differently. 
```
#view 
```{r}
View(df)
##neues fenster - tabelle 
```

#descriptive analysis 
#income 
```{r}
#distribution of income 
hist(df$income)
mean(df$income)
var(df$income)
sd(df$income)

##load df so that it recognises it and then the dollar sign to "pick" a variable"
hist(df$edu)
mean(df$edu)
var(df$edu)
sd(df$edu)
```

#confidence interval
```{r}
upper_95 
```








